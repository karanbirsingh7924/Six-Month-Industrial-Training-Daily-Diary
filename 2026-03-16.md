# Daily Diary: 2026-03-16

## Topic: Setting up the Environment and Core EDA Workflow

Today, I initiated the Daily Diary project focusing on integrating Data Science and LLM pipelines. I set up a local virtual environment and installed core libraries: pandas, numpy, scikit-learn, pydantic, and openai.
I started by performing Exploratory Data Analysis (EDA) on a customer churn dataset. Focused on analyzing distribution plots, checking for null values, and writing a clean helper script to automate loading and summary statistics generation using pandas.

**Code Snippet of the Day:**
```python
import pandas as pd

def load_and_summarize(filepath: str):
    df = pd.read_csv(filepath)
    print(f"Shape: {df.shape}")
    print(df.info())
    return df
```
