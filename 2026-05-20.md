# Daily Diary: 2026-05-20

## Topic: Handling State and Memory in LangGraph

Explored agent memory persistence.
Implemented database-backed checkpointing in LangGraph. This allows the graph state to be serialized and saved to PostgreSQL at each step, enabling session persistence, time-travel (rolling back states), and manual user approval steps.
