# Daily Diary: 2026-04-29

## Topic: Integrating PGVector with PostgreSQL

Moved from ChromaDB to an enterprise-grade solution: PGVector extension in PostgreSQL.
Set up a local PostgreSQL container, enabled pgvector, and created tables containing `vector(1536)` columns.
Wrote raw SQL queries utilizing the cosine distance operator (`<=>`) to perform semantic search queries.
