# Daily Diary: 2026-05-12

## Topic: Deploying Local LLMs (Ollama)

Explored local processing alternatives.
Set up Ollama locally and ran `Mistral-7B` and `Llama3-8B`.
Configured our LLM modules to switch client URL endpoints dynamically depending on configuration. Verified that local models are capable of executing basic extraction tasks when API cost is a concern.
