# Daily Diary: 2026-04-10

## Topic: Cost and Token Optimization Strategies

Studied token tracking and costs.
Implemented a wrapper to calculate prompt tokens, completion tokens, and dollar costs based on model pricing.
Began optimizing system prompts: stripping unnecessary words, using compact XML tags, and evaluating if smaller models (e.g., `gpt-4o-mini` or `llama-3-8b`) can handle simpler tasks instead of expensive frontier models.
