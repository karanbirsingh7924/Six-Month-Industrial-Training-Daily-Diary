# Daily Diary: 2026-05-13

## Topic: Comparing Local Embeddings vs. Cloud Embeddings

Ran local embedding models using HuggingFace's `sentence-transformers` library (specifically `all-MiniLM-L6-v2`).
Compared retrieval accuracy and inference speed against OpenAI's embeddings.
Local embeddings are fast and cost-free, but require careful handling of sequence lengths (limited to 256/512 tokens).
